Scott is a third year computer science student practicing
object oriented programming. He types into the codeServer
application:

    class Adder():

        def add(a, b)
            return a+b

He presses the Save Code Snippet button, clears the text area,
and proceeds to type:

    import numpy
    import Adder

    my_list = numpy.array([5,9])
    adder = Adder()
    sum = adder.add(my_list[0], my_list[1])
    print(sum)

Upon running the code, the server immediately responds with
an error: "Traceback (most recent call last):
  File "", line 1, in
ModuleNotFoundError: No module named 'numpy'"

