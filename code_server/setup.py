from setuptools import setup

setup(
    name="code-server",
    author="G Team",
    discription="Code sender project for COMP 2005",
    package_dir={"": "src"},
    install_requires=[
        # Needs additional requirements
    ],
)